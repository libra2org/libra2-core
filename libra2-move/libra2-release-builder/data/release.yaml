---
remote_endpoint: https://fullnode.mainnet.libra2.org
# replace with below for actual release, compat test needs concrete URL above:
# remote_endpoint: ~
name: "vX.YY.Z"
proposals:
  - name: proposal_1_upgrade_framework
    metadata:
      title: "Multi-step proposal to upgrade mainnet framework, version vX.YY.Z"
      description: "This includes changes in https://github.com/libra2org/libra2-core/releases/tag/libra2-node-vX.YY.Z"
    execution_mode: MultiStep
    update_sequence:
      - Gas:
          new: current
          # replace with below for actual release, above "current" is needed for compat tests:
          # old: https://github.com/libra2org/libra2-networks/main/gas/vX.WW.Z.json
          # new: https://github.com/libra2org/libra2-networks/main/gas/vX.YY.Z.json
      - Framework:
          bytecode_version: 7
          git_hash: ~
